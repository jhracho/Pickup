import React from 'react';
import NavBar from '../Nav/NavBar';

const TeamsPage = () =>{
    return(
        <div>
            <NavBar active='Teams' />
            <h1>Teams</h1>
        </div>
    );
};

export default TeamsPage;