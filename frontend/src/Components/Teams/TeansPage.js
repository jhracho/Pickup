import React from 'react';
import NavBar from '../Nav/NavBar';

const TeamsPage = () =>{
    return(
        <div>
            <NavBar page='Teams' />
            <h1>Teams</h1>
        </div>
    );
};

export default TeamsPage;